import { StyleSheet } from "react-native"

export default StyleSheet.create({
  addToCart: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    height: 50,
    flexDirection: 'row',
    paddingLeft: 10,
    paddingRight: 10,
    paddingBottom: 10
  }
})
